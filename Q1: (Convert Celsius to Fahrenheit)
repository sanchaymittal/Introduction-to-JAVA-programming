/*
Q1: (Convert Celsius to Fahrenheit)
Write a program that reads a Celsius degree in a double value from the console, then converts it to Fahrenheit and displays the result. The formula for the conversion is as follows:
fahrenheit = (9 / 5) * celsius + 32
*/

import java.util.*;        //Import Package Scanner

class Converter{

public static void main(String args[])    //Main class
{
Scanner sc =  new Scanner(System.in);     //Statement helps to read input from the keyboard in Java

double celsius = sc.nextDouble();  //input a double variable


double fahrenheit = (9/5)*celsius + 32;  //Conversion Formula

System.out.println(fahrenheit);  //Output

}
}
